@using Tatneft.Data
@inherits LayoutComponentBase
@inject NavigationManager NavigationManager
@inject AuthenticationStateProvider AuthenticationStateProvider
<div class="sidebar">
    <NavMenu />
</div>

<div class="main">
    <div class="top-row px-4">
        <AuthorizeView>
            <Authorized>
                <a href="/" @onclick="(() => Logout())">Logout</a>
            </Authorized>
            <NotAuthorized>
                <a href="/login">Login</a>
                <a href="/signup">Sign Up</a>
            </NotAuthorized>
        </AuthorizeView>
    </div>

    <div class="content px-4">
        @Body
    </div>
</div>

@code{ 

    //Маркирует пользователя как не аторизованного при выходе из аккаунта
    public async Task Logout()
    {
        //string jsonString;
        //DBWorkingSQLite db = new DBWorkingSQLite();

        ////Создание тела запроса(json) и post запрос, получение токена пользовтелем
        //jsonString = System.Text.Json.JsonSerializer.Serialize(user);
        //HttpClient httpClient = new HttpClient();
        //httpClient.BaseAddress = new Uri("https://localhost:44322");
        //httpClient.PostAsJsonAsync("/api/[controller]/PostTokenClean", user);


        await ((CustomAuthenticationStateProvider)AuthenticationStateProvider).MarkUserAsLoggedOut();
        NavigationManager.NavigateTo("");
    }
   }

